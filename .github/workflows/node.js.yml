name: Test

on:
  push:
    branches: [ 'main' ]
  pull_request:
    branches: [ 'main' ]

jobs:
  setup:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: latest
        cache: 'npm'
  test:
    needs: setup
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - run: npm --version
    - run: npm ci
    - run: npm run docs
    - run: npm run lint
    - run: npm run ll
